# Dockerfile for Vault with custom configuration
FROM vault:latest

# Copy custom configuration if needed
# COPY vault-config.hcl /vault/config/vault-config.hcl

# Expose port
EXPOSE 8200

# Default command
CMD ["vault", "server", "-dev", "-dev-listen-address=0.0.0.0:8200"]
