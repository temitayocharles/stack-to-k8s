apiVersion: v1
kind: Service
metadata:
  name: postgresql
  namespace: medical-care-system
  labels:
    app: postgresql
    component: database
spec:
  selector:
    app: postgresql
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
    name: postgresql
  type: ClusterIP
