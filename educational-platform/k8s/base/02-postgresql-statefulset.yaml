# ====================================
# PostgreSQL StatefulSet for Educational Platform
# ====================================
apiVersion: v1
kind: Service
metadata:
  name: educational-postgresql
  namespace: educational
  labels:
    app: educational-platform
    component: postgresql
spec:
  clusterIP: None  # Headless service for StatefulSet
  selector:
    app: educational-platform
    component: postgresql
  ports:
  - name: postgresql
    port: 5432
    targetPort: 5432

---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: educational-postgresql
  namespace: educational
  labels:
    app: educational-platform
    component: postgresql
spec:
  serviceName: educational-postgresql
  replicas: 1
  selector:
    matchLabels:
      app: educational-platform
      component: postgresql
  template:
    metadata:
      labels:
        app: educational-platform
        component: postgresql
    spec:
      containers:
      - name: postgresql
        image: postgres:15-alpine
        ports:
        - containerPort: 5432
          name: postgresql
        env:
        - name: POSTGRES_DB
          value: "educationaldb"
        - name: POSTGRES_USER
          valueFrom:
            secretKeyRef:
              name: educational-secret
              key: SPRING_DATASOURCE_USERNAME
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: educational-secret
              key: SPRING_DATASOURCE_PASSWORD
        - name: PGDATA
          value: /var/lib/postgresql/data/pgdata
        volumeMounts:
        - name: postgresql-data
          mountPath: /var/lib/postgresql/data
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          exec:
            command:
            - pg_isready
            - -U
            - $(POSTGRES_USER)
            - -d
            - educationaldb
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          exec:
            command:
            - pg_isready
            - -U
            - $(POSTGRES_USER)
            - -d
            - educationaldb
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
  volumeClaimTemplates:
  - metadata:
      name: postgresql-data
      labels:
        app: educational-platform
        component: postgresql
    spec:
      accessModes: ["ReadWriteOnce"]
      resources:
        requests:
          storage: 10Gi
